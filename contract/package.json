{
  "name": "clawcounsel-contract",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network og-testnet",
    "deploy:mainnet": "hardhat run scripts/deploy-real.js --network og-mainnet",
    "flatten": "hardhat flatten contracts/AgentNFT.sol > AgentNFT-flat.sol"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.1.3",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.3.1"
  }
}
